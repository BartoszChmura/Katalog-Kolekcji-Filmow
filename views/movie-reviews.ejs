<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Recenzje dla <%= movie.title %>
    </title>
    <link rel="stylesheet" href="/styles/style.css">
</head>

<body>
    <%- include('partials/nav') %>
        <h1>Recenzje dla <%= movie.title %>
        </h1>
        <% reviews.forEach(review=> { %>
            <p>
                <%= review.content %> - <%= review.username %>
                        <%= review.created_at.toISOString().slice(0, 10) %>
            </p> 
            <% }) %>
                <h2>Dodaj recenzję</h2>
                <form action="/movies/<%= movie.id %>/add-review" method="POST">
                    <input type="text" name="username" placeholder="Twoje imię" required><br>
                    <textarea name="content" required></textarea><br>
                    <button type="submit">Dodaj Recenzję</button>
                </form>
</body>

</html>